# Semaine 13
- Animations du UI : API `view-transition`
- Filtrer un tableau JavaScript avec `filter`
- Méthodes diverses des tableaux et des chaînes de caractères (`join`, `includes`, `trim`, `toLowerCase`)
- Format JSON, API `JSON` de JavaScript, (optionnel : charger un fichier JSON dans le DOM)
- API `LocalStorage`

Objectifs
- Les bases des `view-transition` (CSS seul)
- Contenu dynamique et `view-transition` (CSS/JS)
- Filtrer l'affichage d'une liste par mot-clé
- Sauvegarde et récupération d'état dans `localStorage`
- Optionnel : charger des données externes au format JSON

Consultez le sommaire de la matière vue dans le cours cette semaine : [s13-sommaire-cours.md](s13-sommaire-cours.md)